# 环境变量配置示例
# 复制此文件为 .env.production 并根据实际情况修改

# 应用基础路径
# 如果部署在域名根目录，设置为 '/'
# 如果部署在子目录，例如 '/school-shop-admin/'，则设置为 '/school-shop-admin/'
VITE_APP_BASE=/

# API配置
VITE_APP_BASE_API=/api
VITE_APP_BASE_URL=https://your-api-domain.com

# 开发环境API配置
VITE_APP_BASE_API_DEV=/api
VITE_APP_BASE_URL_DEV=http://localhost:3000

# 路由加载方式
VITE_APP_LOAD_ROUTE_WAY=FRONTEND
